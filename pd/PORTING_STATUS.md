# PD-in-O2S porting status — RETIRED

**Retired 10 September 2026 on Tahir's ruling.** Do not read this file as a
description of PD. It described the July 2026 port of the old PHP `van-rd-app`
into O2S — a gate-based idea/hypothesis pipeline that **no longer exists**. PD
was rebuilt from scratch in September 2026 around the nine-object model
(Problem, Question, Bet, Run, Claim, Challenge, Observation, Request,
Constraint). Almost every screen, table and rule this file recorded has since
been replaced.

It was retired rather than updated because two status documents guarantee one
of them is wrong, and this was the one going stale.

**The current status of PD lives in one place: `../OP-HANDOFF.md`.** Read the
most recent dated entry there.

The model itself is specified in `../docs/pd-model/`:

| File | What it holds |
|---|---|
| `MODEL.md` | the nine objects, the two hard rules (§0), the four signed-off screens (§5) |
| `RECLASSIFICATION-RULES.md` | the no-blame rules — the banned words (§6), the author's one message (§7), why no per-person count is derivable (§8.1) |
| `REUSE-RULES.md` | what may be reused from the old build, and the screen-count tripwire (§5) |
| `PENDING-DECISIONS.md` | questions still open, with Tahir's rulings as they land |
| `combination-bank/RULES.md` | the Combination Bank: entry, the six duplicate signals, the four bands, moderation |

The file's own history is in git if the July port ever needs to be looked up.
