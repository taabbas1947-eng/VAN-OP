services:
  - type: web
    name: van-control-tower
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: van-db
          property: connectionString
databases:
  - name: van-db
    plan: free
