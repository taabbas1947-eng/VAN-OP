# O2S — handoff, 24 August 2026

Written so the next conversation can pick up cold. Plain language throughout.

---

## Where things stand right now

**Nothing has been pushed.** The changes below are sitting in the working tree
at `E:\VAN-OP`, uncommitted. 487 checks pass.

### In the tree, reviewed clean, ready when you say go

**1. Data Fix — six permission gates.**
Six functions could write to the database with no permission check at all:
the four "backfill" writers (production, packing, shipment, PO), the correction
writer, and the button that downloads the whole database as a file. They now all
ask the access matrix the same question: *does this person have Edit on Data
Fix?* Nothing widens — this can only ever refuse somebody who could previously
get through. The COO is never locked out.

I also corrected a comment I had written that was **false**: it claimed five
other Data Fix functions already carried this check and were this one's
"siblings". They were the console controls (the on/off switch, the imports,
the year-end wipe), not the writers. The writers had nothing.

**2. Bulk print-on-pack — the "no price" question.**
The "Set all to no price" button is deleted. And answering "no price" for a batch
of POs now asks you to confirm, naming how many POs, which products normally
carry a price, and what it does to QA. "No" is the one answer that disarms a
control, so it should not be one click.
*Note:* 19 of your 21 open POs are covered. The other 2 have brands that have
never carried a printed price, so there is nothing to warn about — same rule the
single-PO screen already uses.

### Refused on review, reverted, parked

**Customer and dealer ids.** Two attempts, two refusals, both correct.
Full write-up: `docs/o2s/parked-customer-ids/WHAT-WENT-WRONG.md`.

Short version: giving those rows an id is the right fix, but the guard I put on
the merge to make it safe was **generic** — it applied to the whole database,
not just customers. And it made the change trail (`state.audit`) actively worse:
that list is already half-id'd today, and under my guard, two ordinary people
saving in the same minute would have thrown away one of them's audit rows
entirely, silently, while the screen said "synced".

Second, independent problem the reviewer found: customer **codes** are generated
by counting rows, so two people creating a customer at the same time get the
**same code**. Today one of them silently vanishes. With ids, both survive under
one code — and every screen looks customers up by code, so the second one can
never be opened, edited or deleted. A permanent ghost.

Three things have to be fixed before the ids go in, in this order: don't touch
the generic merge; make the audit trail all-id'd or none; make customer codes
unique by construction. That is a real piece of work, not a small one.

**Consequence for you today:** Customer Master stays locked to KAM and the COO.
That lock *is* the protection. Do not widen it in Users & Access.

---

## The review gate is doing its job

Since we started this rule, independent review has refused: two shipment
attempts, four batch rounds, two slip designs, the save-retry, and now the
customer ids twice. Nothing broken has reached your team. Two of those refusals
found faults I had no idea were there.

---

## Still owed from your "LETS GO" list

You named five things. Two are done and in the tree (bulk no-price button,
Data Fix permission check). One is parked with a written reason (customer and
dealer ids). **Two have not been started:**

**The KAM split.** Right now one field does two jobs. The PO Confirmation prints
"Prepared by — KAM", taking whoever was picked in a dropdown. Since the Plant
Manager can now raise a PO, a customer-facing document can name a KAM who never
touched the order. Your rule was clear: the KAM is *who manages this customer*,
not *who typed this PO in*. So: **Account KAM** comes from the customer record,
**Entered by** comes from the login. The customer records' KAM field can be
back-filled from existing orders with no ambiguity and no conflicts.

**The printing slip.** Blocked on one missing option. Today the app knows three
answers to "what price goes on the bag": no price, VAN sets it, customer gives
the number. UDPL sends bags with the price **already printed** — the fourth
answer, `supplied`, where VAN does nothing. Until that exists the slip has no
correct thing to print for them.

---

## Also open, in rough priority order

1. **The 409 baseline defect.** When a save collides, the app records the merged
   result as "what the server has" — but the server has not confirmed it. This
   is the root cause under the parked save-retry work. It is the most dangerous
   thing on this list.
2. **Data Fix removal**, as you asked. `dfSubmitCorrect` duplicates the
   correction ledger. The four backfill writers need a home in that ledger first.
3. **The rights sweep** — Production's 28 gates and admin's 5. Blocked on your
   matrix cells below.
4. **`RIGHTS_LOCKED`** so Users & Access stops implying that a screen Edit grant
   buys a sign-off. It does not, and must not, but the screen reads as if it does.
5. **A small one, now that we know:** `onDlrRegion` and `onDlrCity` have no
   callers anywhere in the file. The dealer form's region and city never reach
   the code generator.
6. **A test hole found by mutation testing:** the check that the "Set all to no
   price" button stays deleted is a text match, so re-adding it with different
   quote marks would slip through. Worth 10 minutes.

---

## What is at YOUR end — nothing here needs code

**A. The Excel role file.** Sent to your managers. Everything in the
authorisation redesign waits on it coming back. Chase it — it is the single
biggest unblocker.

**B. Six matrix cells only you can decide** (`docs/o2s/MATRIX-REVIEW.md`):

1. **Plant Manager holds Edit on Users & Access.** Once the sweep finishes this
   means he can grant himself every right in the system. Suggest: view or none.
2. **QA Inspector holds Edit on Production** — could enter the production he
   later inspects. Suggest: view. **This one cell blocks the biggest part of the
   sweep.**
3. **Supply Chain Officer still holds Edit on Lab QC** — the 30 July grant from
   the incident, still sitting there. Suggest: remove.
4. **Zain is under two role names** ("Supply Chain" and "Supply Chain Officer")
   with *different* rights. One live name, please.
5. **Everyone has Edit on Reports** — 11 of 11 roles. Worth one look.
6. **Ismael:** which role is his login actually assigned to, and does that role
   have **Edit** (not view) on New PO Entry? View shows him the form with a dead
   button — exactly the symptom you reported.

**C. Two answers I need to move the last two items:**

- **The printing slip:** confirm the fourth option is *"customer sends bags with
  the price already printed — VAN prints nothing"* and that UDPL is the only
  customer on it today.
- **The KAM split:** when a customer has no KAM on record and an order needs
  one, should the app leave it blank on the printed confirmation, or fall back to
  whoever entered the PO?

**D. And a decision on what is in the tree:** say the word and the Data Fix
gates and the bulk no-price change get committed. Or hold them and take them
together with the KAM split. Either is fine — they are independent.

---

## Ground rules I am working under

- Plain language, no code-speak. You are not a code person and I should not
  write as if you were.
- Nothing pushes without an independent expert review, every time.
- Always end with what is next at your end.
- Old orders — Rudolf, LCI, Arysta — are closed as past cases. Forward only.
