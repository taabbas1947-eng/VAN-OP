--- a/pd/pd.html
+++ b/pd/pd.html
@@ -100,10 +100,23 @@
     .pdside{width:100%;height:auto;position:static;flex-direction:row;flex-wrap:wrap;align-items:center;gap:6px}
     .pdside .brand{border-bottom:none;margin-bottom:0;padding:3px 6px}
     .pdside .nav{flex-direction:row;flex-wrap:wrap;overflow:visible;flex-basis:100%;order:3}
-    .pdside .navgrp{display:none}
+    /* FIX D13 — this hid the four menu group headings, leaving up to twenty-one undifferentiated
+       links wrapping across the top with no structure at all. They are the only thing telling you
+       which part of the pipeline you are looking at, so on a small screen they matter MORE. */
+    /* flex-basis:100% forces the heading onto its own line so the links that follow sit under it.
+       No `order` — the DOM already interleaves heading, links, heading, links; setting an order
+       collects all four headings at the end, away from the groups they name. */
+    .pdside .navgrp{display:block;flex:0 0 100%;width:100%;margin:10px 0 2px;font-size:11px;opacity:.75}
     .pdside .raillauncher{width:auto;margin-bottom:0}
     .pdside .raillauncher .lb,.pdside .railcollapse{display:none}
     .pdside .foot{border-top:none;margin-top:0;margin-left:auto;padding-top:0}
+    /* FIX D13 — iOS Safari zooms the whole page whenever a field smaller than 16px is tapped, and
+       every input here was 13px. Buttons were ~33px against a 44px touch guideline. */
+    input,textarea,select{font-size:16px !important}
+    .btn{min-height:44px;padding:10px 14px}
+    table{font-size:13px}
+    td,th{padding:8px 6px}
+    h1{font-size:1.2rem}
   }
   h1{font-family:var(--serif);font-size:1.4rem;font-weight:700;letter-spacing:-.02em;margin:0 0 6px;color:var(--text)}
   h2,h3{font-family:var(--serif);letter-spacing:-.02em}
@@ -172,6 +185,31 @@
 }, true);
 
 function esc(s){ return String(s==null?'':s).replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
+
+/* FIX D9 — about two dozen places printed raw database values at people: "heavy", "field_trial",
+   "being_addressed", "bench_done", "qc_head", "superseded", "ideas_screen". Every one of these
+   already has a written label in pd-lib.js on the server; the screens simply were not using them.
+   One table here, used everywhere, so nobody has to learn the database's vocabulary. */
+const LANE_LABEL = { heavy: 'Heavy lane', light: 'Light lane' };
+const STATUS_LABEL = {
+  open: 'Open', being_addressed: 'Being addressed', solved: 'Solved', retired: 'Retired',
+  draft: 'Draft', reviewed: 'Reviewed', approved: 'Approved', bench_done: 'Bench work done', closed: 'Closed',
+  candidate: 'Candidate version', current: 'Current version', superseded: 'Superseded',
+  active: 'Active', parked: 'Parked', launched: 'Launched',
+  designed: 'Designed', running: 'Running', harvested: 'Harvested', analysed: 'Analysed',
+  screened: 'Screened on paper', selected: 'Selected', rejected: 'Rejected', made: 'Made',
+  pass: 'Pass', borderline: 'Borderline', fail: 'Fail',
+  not_started: 'Not started', submitted: 'Submitted', granted: 'Granted',
+  new: 'New', converted: 'Converted', dismissed: 'Dismissed',
+};
+const humanise = v => {
+  const k = String(v == null ? '' : v).trim();
+  if (!k) return '—';
+  if (STATUS_LABEL[k]) return STATUS_LABEL[k];
+  if (LANE_LABEL[k]) return LANE_LABEL[k];
+  // last resort: turn a database key into words rather than showing the key
+  return k.replace(/[_.]+/g, ' ').replace(/^\w/, c => c.toUpperCase());
+};
 function num(v){ const n = Number(v); return Number.isFinite(n) ? String(n) : '0'; } // DECIMALs arrive as strings like "54.000"
 function money(v){ const n = Number(v)||0; return 'Rs ' + Math.round(n).toLocaleString('en-US'); }
 
@@ -344,7 +382,12 @@
 
 async function renderHome(hash){
   shell('<h1>Bring something in</h1><p class="sub">Loading…</p>', hash);
-  let d; try { d = await api('GET','/api/pd/home'); } catch(e){ d = { total:0, isSubmitter:false, submissions:[] }; }
+  /* FIX B10 — this swallowed every failure and drew the reassuring "Nothing logged yet — that is
+     how a zero-data pilot starts" panel. A database outage, a dropped connection and a genuinely
+     empty pilot all looked identical, so a real fault could sit there for days looking like calm. */
+  let d, loadFailed = null;
+  try { d = await api('GET','/api/pd/home'); }
+  catch(e){ loadFailed = e; d = { total:0, isSubmitter:false, submissions:[] }; }
   const door = (href, emoji, title, desc) => '<a href="'+href+'" style="text-decoration:none;color:inherit"><div class="card" style="margin:0;height:100%"><h3 style="margin:0 0 6px;font-size:1rem;color:var(--text)">'+emoji+' '+title+'</h3><p class="sub" style="margin:0">'+desc+'</p></div></a>';
   const doors = '<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px;margin-bottom:14px">'
     + door('#new','💡','Share an idea','A recipe, an ingredient, a route, a tweak, something a farmer said. A good question is enough — it need not be worked out.')
@@ -364,9 +407,14 @@
       + d.submissions.map(x=>'<tr class="clickable" onclick="location.hash=\'#idea/'+x.id+'\'"><td><b>'+x.h_label+'</b></td><td>'+esc(x.title)+'</td>'+(d.isSubmitter?'':'<td class="sub">'+esc(x.submitter_name)+'</td>')+'<td class="sub">'+esc(x.change_type_label)+'</td>'+(d.isSubmitter?'':'<td><span class="chip '+x.lane+'">'+x.lane+'</span></td>')+'<td><span class="chip stage">'+esc(x.stage_label)+'</span></td><td>'+(x.screen_decision?'<b>'+esc(x.screen_decision.toUpperCase())+'</b>':'<span class="sub">awaiting</span>')+'</td></tr>').join('')
       + '</table></div>'
     : '<p class="sub" style="margin:0">Nothing yet. Use a door above — yours could be the first.</p>';
+  const loadWarn = loadFailed
+    ? '<div class="card" style="border-color:#e0b4b4;margin:0 0 14px"><b>The pipeline could not be loaded just now,</b> so this page may be showing nothing when there is something.'
+      + '<div class="sub" style="margin-top:6px">'+esc(loadFailed.message || 'The server did not answer.')+'</div>'
+      + '<div class="sub" style="margin-top:8px">The three doors below still work — anything you submit is saved.</div></div>'
+    : '';
   setMain('<h1>Bring something in</h1>'
     + '<p class="sub">The front door. Everything the system does starts with one of three things going in — each logged, answered with a reason inside 15 days, and remembered forever. You never need permission to put something in.</p>'
-    + doors + dropline + zero
+    + loadWarn + doors + dropline + (loadFailed ? '' : zero)
     + '<h2 style="font-family:var(--serif);font-size:1.15rem;margin:16px 0 8px">'+(d.isSubmitter?'Your submissions':'Recent submissions')+'</h2>'
     + '<div class="card">'+subs+'</div>');
 }
@@ -537,12 +585,52 @@
 async function renderIdeas(hash){
   shell('<h1>Ideas</h1><p class="sub">Loading…</p>', hash);
   const rows = await api('GET','/api/pd/ideas');
-  const trs = rows.map(r =>
-    '<tr class="clickable" onclick="location.hash=\'#idea/'+r.id+'\'"><td>'+r.h_label+'</td><td>'+esc(r.title)+'</td>'
-    + '<td><span class="chip '+r.lane+'">'+r.lane+'</span></td><td><span class="chip stage">'+esc(r.stage_label)+'</span></td>'
-    + '<td>'+esc(r.submitted_by_name)+'</td></tr>').join('');
-  setMain('<h1>Ideas</h1><p class="sub">'+rows.length+' idea(s).</p>'
-    + '<div class="card"><table><tr><th>H#</th><th>Title</th><th>Lane</th><th>Stage</th><th>Submitted by</th></tr>'+trs+'</table></div>');
+
+  /* FIX D11 — H-numbers are printed on every screen in the application and were searchable on
+     none of them. Somebody holding a printed board and the number H-042 had no way to reach it.
+     Matches the number, the title, the stage or the person, and understands "H-42", "H42", "42". */
+  const norm = s => String(s || '').toLowerCase().trim();
+  const hNum = s => { const m = norm(s).match(/^h?-?0*(\d+)$/); return m ? Number(m[1]) : null; };
+  const match = (r, q) => {
+    if (!q) return true;
+    const n = hNum(q);
+    if (n !== null && Number(r.h_number) === n) return true;
+    return [r.h_label, r.title, r.stage_label, r.submitted_by_name, LANE_LABEL[r.lane] || r.lane]
+      .some(v => norm(v).includes(q));
+  };
+
+  const draw = (q) => {
+    const list = rows.filter(r => match(r, q));
+    // FIX D9 — the lane chip printed the raw database value ("heavy" / "light").
+    const trs = list.map(r =>
+      '<tr class="clickable" onclick="location.hash=\'#idea/'+r.id+'\'"><td><b>'+r.h_label+'</b></td><td>'+esc(r.title)+'</td>'
+      + '<td><span class="chip '+r.lane+'">'+esc(LANE_LABEL[r.lane] || r.lane)+'</span></td>'
+      + '<td><span class="chip stage">'+esc(r.stage_label)+'</span></td>'
+      + '<td>'+esc(r.submitted_by_name)+'</td></tr>').join('');
+
+    /* FIX B9 — this was the only list in the application with no empty state. On a new system it
+       drew a table of headings and nothing else, which reads as "broken" rather than "nothing yet".
+       It is also the screen a new person is most likely to open first. */
+    const body = list.length
+      ? '<div style="overflow-x:auto"><table><tr><th>H#</th><th>Title</th><th>Lane</th><th>Stage</th><th>Submitted by</th></tr>'+trs+'</table></div>'
+      : (rows.length
+          ? '<p class="sub" style="margin:0">Nothing matches “'+esc(q)+'”. <a href="#" id="i_clear">Show all '+rows.length+'</a>.</p>'
+          : '<div style="text-align:center;padding:26px 10px">'
+            + '<p style="margin:0 0 6px;font-weight:600">No ideas have been logged yet.</p>'
+            + '<p class="sub" style="margin:0 0 14px">This is how a new pipeline starts. The first one can be two sentences.</p>'
+            + '<a class="btn" href="#new">Share the first idea</a></div>');
+
+    setMain('<h1>Ideas</h1>'
+      + '<p class="sub">'+(rows.length ? (q ? list.length+' of '+rows.length+' shown' : rows.length+' idea'+(rows.length===1?'':'s')) : 'The full pipeline, oldest number first.')+'</p>'
+      + (rows.length ? '<div class="card" style="padding:10px 12px"><input id="i_q" placeholder="Search — H-number, title, stage or person" value="'+esc(q||'')+'" style="margin:0"></div>' : '')
+      + '<div class="card">'+body+'</div>');
+
+    const qi = document.getElementById('i_q');
+    if (qi) { qi.oninput = () => { const v = qi.value; draw(norm(v)); const el = document.getElementById('i_q'); if (el) { el.focus(); el.setSelectionRange(el.value.length, el.value.length); } }; }
+    const cl = document.getElementById('i_clear');
+    if (cl) cl.onclick = (e) => { e.preventDefault(); draw(''); };
+  };
+  draw('');
 }
 
 async function renderMyWork(hash){
@@ -845,7 +933,7 @@
       + '<label>Committee members present (roles)</label><input id="g_att" placeholder="COO, Plant Manager, QC Head…">'
       + '<label>Reason (recorded forever — the owner is told)</label><textarea id="g_reason"></textarea>'
       + '<label style="font-weight:400;display:flex;align-items:center;gap:8px;margin-top:8px"><input type="checkbox" id="g_site" style="width:auto"> Site quorum — Plant Manager presiding under delegated authority (provisional until the COO ratifies within 48 h)</label>'
-      + '<div style="margin-top:12px"><button class="btn" id="g_submit">Record decision</button> <span id="g_msg"></span></div></div>';
+      + '<div style="margin-top:12px"><button class="btn" id="g_submit" data-confirm="Record this gate decision?\n\nGate decisions are kept forever and there is no undo. A KILL closes the idea permanently — it keeps its number and its reason, but it does not come back.\n\nRecord it?">Record decision</button> <span id="g_msg"></span></div></div>';
   }
 
   const logTable = '<div class="card"><h2 style="font-size:1.05rem;margin:0 0 10px">Decision log</h2>'
@@ -881,18 +969,29 @@
   shell('<h1>Samples</h1><p class="sub">Loading…</p>', hash);
   const rows = await api('GET','/api/pd/samples');
   const canLog = ['qc_head','custodian','lab_tech','coo'].includes(me.pd_role);
-  let ideas = [], recs = [];
-  if (canLog) { try { ideas = await api('GET','/api/pd/ideas'); } catch(e){} try { recs = await api('GET','/api/pd/records'); } catch(e){} }
+  /* FIX B6 — the Development Record list is refused to a Lab Technician, and the error was
+     discarded, so the dropdown rendered empty every single time with nothing to explain it.
+     A control that never works and never says why is worse than one that is not there. */
+  let ideas = [], recs = [], recsDenied = false;
+  if (canLog) {
+    try { ideas = await api('GET','/api/pd/ideas'); } catch(e){}
+    try { recs = await api('GET','/api/pd/records'); } catch(e){ if (e && (e.status === 403 || e.status === 401)) recsDenied = true; }
+  }
 
   let form = '';
   if (canLog) {
     const hypOpts = ideas.filter(i => !['Killed','Parked'].includes(i.stage_label)).map(i => '<option value="'+i.id+'">'+i.h_label+' — '+esc(i.title)+'</option>').join('');
     const recOpts = recs.map(r => '<option value="'+r.id+'">'+esc(r.record_no)+'</option>').join('');
+    // FIX B6 — a Lab Technician may not read the record list, so this select is always empty for
+    // them. Say so, instead of leaving a control that looks broken.
+    const recNote = recsDenied
+      ? '<p class="sub" style="margin:4px 0 0">Development Records are not on your menu, so this list stays empty — that is expected. Leave it blank; the QC Head links the record.</p>'
+      : (recs.length ? '' : '<p class="sub" style="margin:4px 0 0">No Development Records exist yet. Leave this blank.</p>');
     form = '<div class="card"><h2 style="font-size:1.05rem;margin:0 0 6px">Log a sample</h2>'
       + '<p class="sub" style="margin:0 0 10px">Only after the idea has a <b>G3 (beaker gate) advance</b> in the gate log.</p>'
       + '<div class="row" style="gap:14px">'
       + '<div style="flex:2;min-width:220px"><label>Hypothesis</label><select id="s_hyp"><option value="">—</option>'+hypOpts+'</select></div>'
-      + '<div style="flex:1;min-width:150px"><label>Development Record</label><select id="s_dr"><option value="">—</option>'+recOpts+'</select></div>'
+      + '<div style="flex:1;min-width:150px"><label>Development Record</label><select id="s_dr"'+(recsDenied?' disabled':'')+'><option value="">—</option>'+recOpts+'</select>'+recNote+'</div>'
       + '<div style="flex:1;min-width:130px"><label>Date made</label><input type="date" id="s_made"></div>'
       + '<div style="flex:1;min-width:150px"><label>Recipe (short)</label><input id="s_recipe"></div>'
       + '<div style="flex:1;min-width:130px"><label>Target analysis</label><input id="s_target"></div>'
@@ -1095,7 +1194,7 @@
       let act = '';
       if (caps.decide && ['screened','rejected'].includes(c.status)) act += '<div style="margin-top:8px;display:flex;gap:8px;flex-wrap:wrap;align-items:center"><input id="dr_'+c.id+'" placeholder="Reason (kept forever)" style="flex:1;min-width:200px"><button class="btn" data-decide="'+c.id+'" data-want="selected">Select for bench</button><button class="btn ghost" data-decide="'+c.id+'" data-want="rejected">Rule out</button></div>';
       if (c.decision_reason) act += '<div class="sub" style="margin-top:6px"><b>'+esc((c.status||'').toUpperCase())+':</b> '+esc(c.decision_reason)+(c.decided_by_name?' — '+esc(c.decided_by_name):'')+'</div>';
-      if (caps.make && c.status==='selected') act += '<div style="margin-top:8px"><button class="btn'+(g3_open?'':' ghost')+'" data-make="'+c.id+'">Make this — log a sample</button>'+(g3_open?'':' <span class="sub">the beaker gate is shut; this will be refused</span>')+'</div>';
+      if (caps.make && c.status==='selected') act += '<div style="margin-top:8px"><button class="btn'+(g3_open?'':' ghost')+'" data-confirm="Make this candidate?\n\nThis creates a permanent numbered sample record and commits materials and bench time. It cannot be undone.\n\nMake it?" data-make="'+c.id+'">Make this — log a sample</button>'+(g3_open?'':' <span class="sub">the beaker gate is shut; this will be refused</span>')+'</div>';
       const reasons = '<tr><td colspan="10" style="background:var(--surface-2)"><span class="sub" style="white-space:pre-wrap">'+esc(c.verdict_reasons||'')+'</span>'+act+'</td></tr>';
       return main + reasons;
     }).join('');
